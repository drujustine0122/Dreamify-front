import { Route } from '@angular/router';
import { CompleteProfileComponent } from './complete-profile.component';

export const completeProfileRoutes: Route[] = [
  {
    path: '',
    component: CompleteProfileComponent
  }
];
